<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All favorite recipes
    </h1>

    <a href="/favorite_recipes/new" class="btn btn-block btn-outline-secondary">
      Add a new favorite recipe
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Recipe
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @favorite_recipes.each do |favorite_recipe| %>
      <tr>
        <!-- Display favorite_recipe.id start -->
        <td>
          <%= favorite_recipe.id %>
        </td>
        <!-- Display favorite_recipe.id end -->

        <!-- Display favorite_recipe.user_id start -->
        <td>
          <% if favorite_recipe.user.present? %>
            <a href="/users/<%= favorite_recipe.user.id %>">
              <%= favorite_recipe.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display favorite_recipe.user_id end -->

        <!-- Display favorite_recipe.recipe_id start -->
        <td>
          <% if favorite_recipe.recipe.present? %>
            <a href="/recipes/<%= favorite_recipe.recipe.id %>">
              <%= favorite_recipe.recipe.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display favorite_recipe.recipe_id end -->

        <!-- Display favorite_recipe.created_at start -->
        <td>
          <%= time_ago_in_words(favorite_recipe.created_at) %> ago
        </td>
        <!-- Display favorite_recipe.created_at end -->

        <!-- Display favorite_recipe.updated_at start -->
        <td>
          <%= time_ago_in_words(favorite_recipe.updated_at) %> ago
        </td>
        <!-- Display favorite_recipe.updated_at end -->

        <td>
          <a href="/favorite_recipes/<%= favorite_recipe.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @favorite_recipes, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
